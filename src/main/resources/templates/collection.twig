<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <meta name="description" content="Quest Store">
    <meta name="keywords" content="web, design, quest store, html, css, js, java, sql">
    <meta name="author" content="Patryk Lichwała, Krzysztof Czaja">
    <title>Collection | View</title>
    <link rel="stylesheet" href="/static/css/style.css">
</head>
<body>
    <div class="container">
        <div id="logout">
            <p>Logged as: {{ fullName }} <span class="coins">{{ coins }}</span></p>
            <form method="GET">
                <button type="submit" formaction="/logout">Logout</button>
            </form>
        </div>
            <header>
                <h1 >Quest Store</h1>
            </header>
        <nav>
            <ul class="menu">
                <li><a href="/user">Store</a></li>
                <li><a href="/collection">Collection</a></li>
                <li><a href="/profile">Profile</a></li>
            </ul>
        </nav>
        <section id="content">
            <div class="item wider">
                <section class="artifact-head">
                    <h2>{{ artifact.getName() }}</h2>
                    <h3>{{ collectionDescription }}</h3>
                    <p class="coins">{{ artifact.getPrice() }}</p>
                    <img src="{{ artifact.getImageLink() }}" alt="{{ artifact.getName() }}">
                    <div class="progress-border">
                        <div class="progress-bar" style="width: {{ coinsPercentageAmount }}%" id="collectionID_1">{{ coinsPercentageAmount }}%</div>
                    </div>
                    <p>{{ coinsCollected }} of {{ coinsTotal }} coins</p>
                    <form method="POST">
                        <input type="hidden" id="collectionId" name="collectionId" value="{{ collectionId }}">
                        <input type="number" name="donationAmount" id="donationAmount" placeholder="Coins amount">
                        <button type="submit" formaction="/collection/{{ operation }}">Donate</button>
                    </form>
                </section>
                <section class="artifact-description">
                    <p>{{ artifact.getDescription() }}</p>
                </section>
            </div>
        </section>
        <footer>
            <p>Chlur&Chlurs Company &copy; 2019</p>
        </footer>
    </div>
</body>
</html>