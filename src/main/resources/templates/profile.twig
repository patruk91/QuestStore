<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <meta name="description" content="Quest Store">
    <meta name="keywords" content="web, design, quest store, html, css, js, java, sql">
    <meta name="author" content="Patryk Lichwała, Krzysztof Czaja">
    <title>Quest Store | Codecooler</title>
    <link rel="stylesheet" href="/static/css/style.css">
</head>
<body>
    <div class="container">
        <div id="logout">
            <p>Logged as: {{ fullName }} <span class="coins">{{ coins }}</span></p>
            <form method="GET">
                <button type="submit" formaction="/logout">Logout</button>
            </form>
        </div>
        <header>
            <h1 >Quest Store</h1>
        </header>
        <nav>
            <ul class="menu">
                <li><a href="/user">Store</a></li>
                <li><a href="/collection">Collection</a></li>
                <li><a href="/profile">Profile</a></li>
            </ul>
        </nav>
        <section id="content">
            <div class="user-stats item wider">
               <p id="level">{{ expLevelName }}</p>
               <div class="progress-border">
                    <div class="progress-bar" style="width: {{ expPercentageAmount }}%"
                         id="collectionID_1">{{ expPercentageAmount }}%</div>
                </div>
                <p id="exp-need">Exp to next level: {{ expPointsNeed }}</p>
                <p id="total-ext">Total exp: {{ expPointAmount }}</p>
            </div>
            
            <div class="results item" id="result">
                <h3>Artifacts userd</h3>
                {% for artifact in artifacts %}
                <p>{{ loop.index }}. {{ artifact.getName() }}</p>
                {% endfor %}
            </div>
        </section>
        <footer>
            <p>Chlur&Chlurs Company &copy; 2019</p>
        </footer>
    </div>
</body>
</html>